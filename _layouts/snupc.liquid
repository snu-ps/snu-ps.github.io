---
layout: default
---

<div class="post">
  <header class="post-header">
    <h1 class="post-title">{{ page.title }}</h1>

    <strong style="font-size: 140%">
      <p>
        서울대학교 프로그래밍 경시대회<br>
        Sponsored by
        {% for sponsor in page.sponsors %}
          <img src="{{ sponsor.logo }}" alt="{{ sponsor.name }}" class="img-fluid" 
            style="height: 0.9em; vertical-align: middle; margin-left: 10px;">
        {% endfor %}
      </p>
    </strong>

    <strong style="font-size: 110%">
      {% if page.infos %}      
        <ul style="list-style: none; padding-left: 0; margin-bottom: 5px;">
          {% for info in page.infos %}
            <li style="display: inline-block; margin-right: 5px;">
              <a href={{ info.link }}>{{ info.name }}</a>
            </li>
          {% endfor %}
        </ul>
      {% endif %}

      {% if page.div1infos %}
        <div style="display: flex">
          Div1:
          <ul style="list-style: none; padding-left: 5px; margin-bottom: 5px;">
            {% for info in page.div1infos %}
              <li style="display: inline-block; margin-right: 5px;">
                <a href={{ info.link }}>{{ info.name }}</a>
              </li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}

      {% if page.div2infos %}
        <div style="display: flex">
          Div2:
          <ul style="list-style: none; padding-left: 5px; margin-bottom: 5px;">
            {% for info in page.div2infos %}
              <li style="display: inline-block; margin-right: 5px;">
                <a href={{ info.link }}>{{ info.name }}</a>
              </li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}
    </strong>
  </header>

  <hr>

  <article>
    {{ content }}
  </article>
</div>
